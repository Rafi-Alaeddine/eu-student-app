import 'package:flutter/material.dart';

class LanguageHelperPage extends StatelessWidget {
  const LanguageHelperPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
